# Roadmap: SaaS-CRM-Dashboard (BeautyFlow)

> Создано: 2026-01-16
> Последнее обновление: 2026-01-16

---

## ✅ Фаза 1: Первоначальная настройка (Завершено)

- [x] Инициализация репозитория, подключение к GitHub
- [x] Настройка Supabase клиента (`lib/supabase.ts`)
- [x] Создание схемы БД (`supabase/schema.sql`)
- [x] TypeScript типы для таблиц (`types/database.ts`)
- [x] CRUD функции (`lib/db.ts`)
- [x] Интеграция CRUD с UI: Clients, Services, Appointments, Settings

---

## ✅ Фаза 2: Оживление UI (Завершено)

- [x] Dashboard: кнопка "View Calendar" с навигацией
- [x] Header: глобальный поиск (фильтрует клиентов)
- [x] Clients: редактирование и удаление клиентов
- [x] Settings: загрузка/сохранение настроек в Supabase

---

## 🔄 Фаза 3: Исправление критических проблем (Текущая)

### Приоритет 1: Критические баги

- [ ] **Исправить маппинг данных Appointments**
  - Файл: `app/page.tsx` (строки 56-67)
  - Проблема: `clientName` берётся из `master_name`, `service` из `duration`
  - Решение: Добавить JOIN-запрос в `lib/db.ts` или загрузить справочники клиентов/услуг

- [ ] **Синхронизировать состояние даты**
  - Файлы: `app/page.tsx`, `mobile-header.tsx`, `calendar-view.tsx`
  - Проблема: Каждый компонент имеет свой `currentDate`, они не связаны
  - Решение: Поднять `currentDate` в `page.tsx`, передавать через пропсы

- [ ] **Оживить навигацию по датам в Header**
  - Файл: `mobile-header.tsx` (строки 48-55)
  - Проблема: Кнопки `<ChevronLeft>` и `<ChevronRight>` не имеют обработчиков
  - Решение: Передать `onPrevDay` / `onNextDay` из `page.tsx`

### Приоритет 2: Логические ошибки

- [ ] **Рассчитывать `end_time` для записей**
  - Файл: `components/dialogs/new-booking-dialog.tsx` (строка 106)
  - Проблема: `end_time` равен `time`, не учитывает `duration`
  - Решение: Парсить `duration` и добавлять к времени начала

- [ ] **Привязать создание записи к выбранной дате**
  - Файл: `app/page.tsx` (строки 79-101)
  - Проблема: Новые записи всегда создаются на сегодня
  - Решение: Передавать `selectedDate` из CalendarView в BookingDialog

---

## ⬜ Фаза 4: UI заглушки (Следующий этап)

- [ ] **Кнопка "Add Service"** — `services-view.tsx` (строка 136-142)
- [ ] **Пункт меню "View Profile"** — `clients-view.tsx` (строка 216)
- [ ] **Кнопка "Bell" (уведомления)** — `mobile-header.tsx` (строка 68-71)
- [ ] **Загрузка логотипа** — `settings-view.tsx` (строки 163-187)
- [ ] **Режим "Week"** — `calendar-view.tsx` (строка 95)

---

## ⬜ Фаза 5: Dashboard с реальными данными

- [ ] Заменить mock-данные на реальные запросы к Supabase
- [ ] Реализовать агрегацию: выручка, средний чек, no-show rate
- [ ] Добавить фильтрацию по мастерам и услугам

---

## ⬜ Фаза 6: Уведомления и аутентификация

- [ ] Добавить Supabase Auth (email/password или OAuth)
- [ ] Реализовать RLS политики на основе user_id
- [ ] Интегрировать уведомления (email/SMS) через Supabase Edge Functions

---

## ⬜ Фаза 7: Инфраструктура и качество

- [ ] Настроить ESLint + Prettier
- [ ] Добавить unit-тесты (Vitest)
- [ ] Добавить e2e тесты (Playwright)
- [ ] CI/CD pipeline (GitHub Actions)
- [ ] Удалить неиспользуемые файлы (`add-client-sheet.tsx`, `appointment-list.tsx`, `kpi-cards.tsx`)

---

## Легенда статусов

- ✅ Завершено
- 🔄 В работе
- ⬜ Запланировано
- ❌ Отменено

---

## Связанные документы

- [ARCHITECTURE.md](./ARCHITECTURE.md) — Техническая архитектура проекта
- [DEV_LOG.md](./DEV_LOG.md) — Журнал разработки
